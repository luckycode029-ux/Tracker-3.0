[build]
  command = "npm run build"
  functions = "netlify/functions"
  publish = "dist"

[dev]
  functions = "netlify/functions"

# SPA routing redirects
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
